server:
  port: 6666
spring:
  config:
    import: optional:classpath:.env[.properties]
  main:
    allow-bean-definition-overriding: true
  ai:
    openai:
      api-key: ${DEEPSEEK_API_KEY}
      chat:
        options:
          model: deepseek-chat
      base-url: https://api.deepseek.com
      embedding:
#        enabled: true
        options:
          model: BAAI/bge-m3
#          max-token: 512/
          dimensions: 1024
        api-key: ${SILICONFLOW_API_KEY}
        base-url: https://api.siliconflow.cn
#    mcp:
#      client:
#        toolcallback:
#          enabled: true
#        name: mcp-client
#        sse:
#          connections:
#            server1:
#              url: http://localhost:8085/mcp/book
#        stdio:
#          servers-configuration: classpath:/book-stdio-server.json
#        enabled: true
#        type: sync
    vectorstore:
      pgvector:
        index-type: HNSW
        distance-type: COSINE_DISTANCE
        dimensions: 1024
        max-document-batch-size: 10000 # Optional: Maximum number of documents per batch
  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:postgresql://localhost:5432/vector
    username: postgres
    password: 123456
#    dashscope:
#      chat:
#        api-key: ${DASHSCOPE_API_KEY}
#        options:
#          model: deepseek-chat
#          temperature: 0.6 # 模型温度，数值越大 结果越随机
#        base-url: https://api.deepseek.com
#      api-key: ${DASHSCOPE_API_KEY}